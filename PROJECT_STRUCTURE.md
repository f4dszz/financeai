# FinanceAI Platform - 项目结构文档

**版本**: 1.1
**最后更新**: 2024-12-14 22:30
**用途**: Knowledge Transfer / Demo / Presentation

---

## 1. 目录结构 / Directory Structure

```
PythonProject/
├── .github/
│   └── workflows/
│       └── ci.yml                 # GitHub Actions CI配置
├── .venv/                         # Python虚拟环境 (Python 3.14.0)
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py               # FastAPI入口 (/health, /disclaimer)
│   │   ├── agents/               # AI Agent模块 (Week 2+)
│   │   │   ├── __init__.py
│   │   │   ├── ingestion/        # 文档摄入Agent
│   │   │   └── support/          # 客服支持Agent
│   │   ├── api/                  # API路由 (Week 2+)
│   │   │   └── __init__.py
│   │   ├── services/             # 业务逻辑层
│   │   │   └── __init__.py
│   │   └── utils/                # 工具函数
│   │       └── __init__.py
│   ├── Dockerfile
│   └── requirements.txt          # 后端专用依赖
├── frontend/
│   ├── Home.py                   # Streamlit主页
│   ├── pages/
│   │   ├── 1_QA.py              # RAG问答页 (Week 2)
│   │   ├── 2_Chart.py           # 图表识别页 (Week 3)
│   │   └── 3_Anomaly.py         # 异常检测页 (Week 4)
│   └── Dockerfile
├── data/
│   ├── knowledge_base/           # RAG知识库文档
│   ├── sample_charts/            # 图表样本
│   └── synthetic_trades/         # 合成交易数据
├── tests/
│   ├── __init__.py
│   ├── conftest.py              # pytest fixtures
│   └── test_api.py              # API测试
├── .gitignore
├── docker-compose.yml            # 容器编排
├── LICENSE                       # MIT License
├── pyproject.toml               # 项目配置 (ruff/mypy/pytest)
├── requirements.txt             # 根目录依赖
├── README.md                    # 项目说明
├── Project_Plan.md              # 项目计划 v2.3
├── PLAN_REVIEW.md               # 计划复审记录
└── REVIEW_RESPONSE.md           # 复审回复
```

---

## 2. 技术栈 / Tech Stack

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| Runtime | Python | 3.14.0 | 运行时环境 |
| Backend | FastAPI | 0.124+ | REST API框架 |
| Frontend | Streamlit | 1.52+ | Web UI |
| AI/LLM | LangChain + LangGraph | 0.1+ | Agent编排 |
| Vector DB | ChromaDB | 0.4+ | 向量存储 |
| Linting | Ruff | 0.14+ | 代码检查 |
| Testing | Pytest | 9.0+ | 单元测试 |
| Container | Docker Compose | - | 容器编排 |

---

## 3. 设计亮点 / Design Highlights

### 3.1 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    Streamlit Frontend                    │
│              (Home.py + pages/*.py)                     │
└─────────────────────┬───────────────────────────────────┘
                      │ HTTP/REST
┌─────────────────────▼───────────────────────────────────┐
│                    FastAPI Backend                       │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │   /health   │ │  /api/rag   │ │ /api/chart  │       │
│  └─────────────┘ └──────┬──────┘ └─────────────┘       │
│                         │                                │
│  ┌──────────────────────▼──────────────────────────┐   │
│  │              LangGraph Agents                    │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │Ingestion │  │ Support  │  │Operations│      │   │
│  │  │  Agent   │  │  Agent   │  │  Agent   │      │   │
│  │  └──────────┘  └──────────┘  └──────────┘      │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                   Data Layer                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ ChromaDB │  │  SQLite  │  │   Files  │              │
│  │ (Vector) │  │ (Struct) │  │  (Raw)   │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

### 3.2 代码亮点

| 特性 | 实现 | 说明 |
|------|------|------|
| **现代Python** | Python 3.14 + Type Hints | 使用最新语法特性 |
| **Lifespan管理** | `asynccontextmanager` | 替代deprecated `on_event` |
| **时区安全** | `datetime.now(UTC)` | 避免`utcnow()`废弃警告 |
| **双语免责声明** | 内置 `/disclaimer` 端点 | 合规必备 |
| **CI/CD就绪** | GitHub Actions | lint → test → docker build |
| **分层测试** | pytest + fixtures | 可扩展测试框架 |

### 3.3 配置管理

```toml
# pyproject.toml 统一配置
[tool.ruff.lint]      # 代码检查规则
[tool.mypy]           # 类型检查配置
[tool.pytest]         # 测试配置
[tool.coverage]       # 覆盖率配置
```

---

## 4. 优缺点分析 / Pros & Cons

### 优点 / Pros

| 方面 | 优点 |
|------|------|
| **可维护性** | 清晰的分层架构，模块职责单一 |
| **可扩展性** | Agent模块化设计，易于添加新功能 |
| **开发体验** | ruff自动格式化，快速反馈循环 |
| **部署简单** | Docker Compose一键启动 |
| **合规内置** | 免责声明API，满足金融监管要求 |
| **文档完整** | 双语README，计划文档齐全 |

### 缺点 / Cons

| 方面 | 缺点 | 缓解措施 |
|------|------|---------|
| **Python 3.14** | 较新版本，部分库兼容性未知 | 核心库已验证通过 |
| **无认证** | 当前无用户认证 | MVP后添加JWT |
| **单机部署** | 无分布式支持 | 可后期添加Kafka |
| **硬编码配置** | 部分配置未外置 | 使用python-dotenv |

---

## 5. 运行验证 / Verification

### 5.1 环境要求

```bash
# 激活虚拟环境
source .venv/Scripts/activate  # Git Bash
# 或
.\.venv\Scripts\Activate.ps1   # PowerShell
```

### 5.2 验证命令

```bash
# 测试
.venv/Scripts/python.exe -m pytest tests/ -v
# 预期: 2 passed

# Lint检查
.venv/Scripts/python.exe -m ruff check backend/
# 预期: All checks passed!

# FastAPI启动
.venv/Scripts/python.exe -m uvicorn backend.app.main:app --reload
# 访问: http://localhost:8000/docs

# Streamlit启动
.venv/Scripts/streamlit run frontend/Home.py
# 访问: http://localhost:8501
```

### 5.3 Docker启动

```bash
docker-compose up -d
# Backend: http://localhost:8000
# Frontend: http://localhost:8501
```

---

## 6. 当前状态 / Current Status

| 周次 | 模块 | 状态 | 验证结果 |
|------|------|------|---------|
| Week 1 | 工程基础 | ✅ 完成 | pytest 2/2 passed |
| Week 2 | RAG问答 | 🔲 待开发 | - |
| Week 3 | 图表识别 | 🔲 待开发 | - |
| Week 4 | 异常检测 | 🔲 待开发 | - |

---

## 7. 快速入门 / Quick Start

```bash
# 1. 克隆项目
git clone git@github.com:f4dszz/financeai.git
cd financeai

# 2. 激活环境
source .venv/Scripts/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 运行测试
pytest tests/ -v

# 5. 启动服务
uvicorn backend.app.main:app --reload
```

---

## 8. Git配置指南 / Git Configuration Guide

> **配置时间**: 2024-12-14 22:30
> **更新时间**: 2024-12-14 22:45
> **配置位置**: 项目根目录

### 8.1 当前仓库配置

```bash
# 远程仓库地址 (SSH)
origin  git@github.com:f4dszz/financeai.git (fetch)
origin  git@github.com:f4dszz/financeai.git (push)

# 用户信息
user.name: f4dszz
user.email: sding23@fordham.edu

# 默认分支
master
```

### 8.2 SSH密钥配置 (首次使用必读)

使用SSH方式推送代码前，需要配置SSH密钥：

**Step 1: 检查现有密钥**
```bash
ls -la ~/.ssh/
# 查找 id_rsa.pub 或 id_ed25519.pub
# 如果存在，跳到 Step 4
```

**Step 2: 生成新密钥 (如果不存在)**
```bash
ssh-keygen -t ed25519 -C "sding23@fordham.edu"
# 按回车接受默认路径: ~/.ssh/id_ed25519
# 可设置密码保护或直接回车留空
```

**Step 3: 启动SSH代理并添加密钥**
```bash
# Git Bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# PowerShell (需管理员权限)
Start-Service ssh-agent
ssh-add $env:USERPROFILE\.ssh\id_ed25519
```

**Step 4: 复制公钥**
```bash
cat ~/.ssh/id_ed25519.pub
# 复制输出的全部内容 (以 ssh-ed25519 开头)
```

**Step 5: 添加到GitHub**
1. 访问: https://github.com/settings/keys
2. 点击 "New SSH key"
3. Title: 自定义名称 (如 `Work-Laptop`)
4. Key type: Authentication Key
5. Key: 粘贴公钥内容
6. 点击 "Add SSH key"

**Step 6: 验证连接**
```bash
ssh -T git@github.com
# 成功输出: Hi f4dszz! You've successfully authenticated, but GitHub does not provide shell access.
```

### 8.3 查看当前配置

```bash
# 查看远程仓库
git remote -v

# 查看当前分支
git branch

# 查看仓库状态
git status
```

### 8.3 修改远程仓库地址

如需更换到其他仓库（如个人Fork或新仓库）：

```bash
# 方法1: 直接修改URL
git remote set-url origin <新的仓库URL>

# 方法2: 删除后重新添加
git remote remove origin
git remote add origin <新的仓库URL>

# 验证修改
git remote -v
```

**URL格式示例**：
| 类型 | 格式 | 示例 |
|------|------|------|
| SSH | `git@github.com:用户名/仓库名.git` | `git@github.com:f4dszz/financeai.git` |
| HTTPS | `https://github.com/用户名/仓库名.git` | `https://github.com/f4dszz/financeai.git` |

### 8.4 日常Git操作

```bash
# 拉取最新代码
git pull origin master

# 提交更改
git add .
git commit -m "feat: 功能描述"
git push origin master

# 创建新分支开发
git checkout -b feature/新功能名
# ... 开发完成后 ...
git push -u origin feature/新功能名
```

### 8.5 .gitignore 配置说明

以下文件/目录已配置为忽略，不会被提交：

| 忽略项 | 说明 |
|--------|------|
| `.venv/` | Python虚拟环境（每人本地创建） |
| `.idea/` | PyCharm配置（IDE个人设置） |
| `.claude/` | Claude Code会话数据 |
| `main.py` | PyCharm模板文件 |
| `__pycache__/` | Python编译缓存 |
| `.env` | 环境变量（含敏感信息） |
| `*.db` | 本地数据库文件 |
| `chroma_data/` | ChromaDB向量数据 |

### 8.6 首次克隆后配置

新成员克隆项目后需执行：

```bash
# 1. 克隆项目
git clone git@github.com:f4dszz/financeai.git
cd financeai

# 2. 创建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境
source .venv/Scripts/activate  # Git Bash
# 或
.\.venv\Scripts\Activate.ps1   # PowerShell

# 4. 安装依赖
pip install -r requirements.txt

# 5. 验证安装
pytest tests/ -v
```

---

## 9. 联系与支持 / Contact

- **项目负责人**: [待填写]
- **技术支持**: [待填写]
- **文档更新**: 每周随功能更新

---

*本文档将随项目进展持续更新 / This document will be updated as the project progresses*
